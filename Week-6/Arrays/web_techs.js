const webTechs = [
    'HTML',
    'CSS',
    'JavaScript',
    'React',
    'Redux',
    'Node',
    'MongoDB'
  ]

  if(countries.indexOf("Saas") != -1){
    console.log("Sass is a CSS preprocess");
}
else{
    countries.push("Sass");
}

